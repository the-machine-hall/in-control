# Configuration
set _hostname "123.123.123.123"
# The XSCF shell uses a telnet interface on TCP port 8010
set _xscfShellPort "8010"

# Credentials (Before use the password for the "root" account needs to be set via `madmin` which is part of the Enhanced Service Facility (ESF))
set _username "root"
set _password ""
